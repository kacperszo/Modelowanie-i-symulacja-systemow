[project]
name = "dihedral-agents"
version = "0.1.0"
description = "Agent-based model of small molecule conformational sampling"
requires-python = ">=3.11"
dependencies = [
    "mesa>=3.0",
    "rdkit>=2023.9",
    "networkx>=3.0",
    "numpy>=1.24",
    "matplotlib>=3.7",
]
